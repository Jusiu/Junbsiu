# **USER REQUIRED** — local push (box token cannot write)

Repo: https://github.com/Jusiu/Junbsiu (empty public)

```bash
# Option A: from tarball
mkdir -p ~/Junbsiu && cd ~/Junbsiu
tar -xzf Junbsiu-t3n.tar.gz
git init -b main
git add -A
git commit -m "Initial T3N support triage agent for Superteam challenge"
git remote add origin https://github.com/Jusiu/Junbsiu.git
git push -u origin main
```

Then create public Google Doc from `docs-contest/SUBMISSION_DOC.md`, add screenshots, fill DID/email, submit on Earn (HUMAN_ONLY) before 2026-09-16 23:59 Asia/Taipei.
